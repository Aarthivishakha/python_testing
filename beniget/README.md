# Beniget fixture

Data-flow fixture for Beniget 0.5.0. The sample contains intentionally unused
definitions, and `run_beniget.py` reports its def-use chains and dead names.

Run with `python run_beniget.py` after installing the listed requirements.
