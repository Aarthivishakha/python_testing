# Cognitive AST fixture

This fixture uses Complexipy's AST-based analysis to measure cognitive
complexity. `complexity_sample.py` contains four levels of nested conditional
logic designed to produce a high cognitive-complexity score.

Install and run:

```bash
python -m pip install "complexipy>=1.0.0"
bash run_complexipy.sh
```
