# Cosmic Ray fixture

Mutation-testing fixture for Cosmic Ray 8.4.6. `calculator.py` provides two
small mutation targets, while `test_calculator.py` contains tests intended to
kill arithmetic and comparison mutants.

Run with `bash run_cosmic_ray.sh` after installing Cosmic Ray and pytest.
