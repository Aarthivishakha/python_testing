# CrossHair fixture

This fixture uses CrossHair symbolic execution to verify PEP 316-style
preconditions and postconditions on type-annotated Python functions.

Install and run:

```bash
python -m pip install "crosshair-tool>=0.0.109"
bash run_crosshair.sh
```
