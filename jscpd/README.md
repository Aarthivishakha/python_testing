# jscpd trigger fixture

This fixture intentionally contains duplicated code in
`retail_order_processor.py` and `wholesale_order_processor.py` so jscpd reports
a cross-file clone.

Run it with:

```bash
bash run_jscpd.sh
```
