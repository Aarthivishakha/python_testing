# pip-audit fixture

Dependency-vulnerability fixture for pip-audit 2.10.1. The requirements file
intentionally pins outdated dependencies with published vulnerabilities.

Run with `bash run_pip_audit.sh` after installing pip-audit.
