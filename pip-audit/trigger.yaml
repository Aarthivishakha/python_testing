tool: pip-audit
version: "2.10.1"
language: python
technique: "Dependency Risk (SCA)"
classification: "Vulnerability Dependency Detection"
target_files:
  - requirements.txt
run:
  command: "bash run_pip_audit.sh"
requirements:
  - pip-audit==2.10.1
expects:
  - known_cve_count_gt_0: true
