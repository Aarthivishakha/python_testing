# PyMCDC fixture

MC/DC condition-coverage fixture for PyMCDC 0.2.5. The sample contains a
compound `and`/`or` decision, and its test combinations independently exercise
each condition's effect on the outcome.

Run with `bash run_pymcdc.sh` after installing PyMCDC.
